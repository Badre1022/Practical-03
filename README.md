# Practical-03
Perform email configuration to send query received conformation to client and the actual query to admin once a client submits a query via an HTML form.  Upload all work done to complete the above task.
